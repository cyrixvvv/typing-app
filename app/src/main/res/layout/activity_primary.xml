<?xml version="1.0" encoding="utf-8"?>
<!-- Primary mode layout: weight-balanced for small TV screens -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/bg_primary"
    android:padding="@dimen/padding_standard">

    <!-- Top bar: back button + title -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <TextView
            android:id="@+id/tv_back"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/primary_back"
            android:textColor="@color/text_secondary"
            android:textSize="@dimen/text_size_hint"
            android:padding="12dp"
            android:clickable="true"
            android:focusable="true"
            android:background="?android:attr/selectableItemBackground" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/menu_primary"
            android:textColor="@color/text_primary"
            android:textSize="@dimen/text_size_primary"
            android:textStyle="bold"
            android:gravity="center" />

        <View
            android:layout_width="60dp"
            android:layout_height="1dp" />
    </LinearLayout>

    <!-- Status bar: WPM, accuracy, score -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:paddingTop="4dp"
        android:paddingBottom="4dp">

        <TextView
            android:id="@+id/tv_wpm"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="WPM: 0"
            android:textColor="@color/accent"
            android:textSize="@dimen/text_size_stats" />

        <View
            android:layout_width="16dp"
            android:layout_height="1dp" />

        <TextView
            android:id="@+id/tv_accuracy"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="正确率: 0%"
            android:textColor="@color/color_correct"
            android:textSize="@dimen/text_size_stats" />

        <View
            android:layout_width="16dp"
            android:layout_height="1dp" />

        <TextView
            android:id="@+id/tv_score"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="得分: 0"
            android:textColor="@color/color_gold"
            android:textSize="@dimen/text_size_stats" />
    </LinearLayout>

    <!-- Typing text display area + keyboard (split remaining space 3:4) -->
    <TextView
        android:id="@+id/tv_display_text"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="3"
        android:textColor="@color/text_primary"
        android:textSize="@dimen/text_size_primary"
        android:gravity="center"
        android:lineSpacingExtra="6dp"
        android:padding="12dp"
        android:text="按下空格键开始" />

    <!-- Simple progress line -->
    <TextView
        android:id="@+id/tv_progress_hint"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="20sp"
        android:textColor="@color/text_secondary"
        android:gravity="center"
        android:padding="4dp" />

    <!-- Hint: Space to start -->
    <TextView
        android:id="@+id/tv_hint"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/primary_hint"
        android:textColor="@color/text_hint"
        android:textSize="20sp"
        android:gravity="center"
        android:padding="4dp" />

    <!-- Divider -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/text_hint"
        android:layout_marginTop="4dp"
        android:layout_marginBottom="4dp" />

    <!-- Keyboard area -->
    <com.honglu.typing.ui.KeyboardView
        android:id="@+id/keyboard_view"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="4"
        android:focusable="true"
        android:background="@color/bg_secondary" />

</LinearLayout>